class Network:
    ''' This is receiving sorted data with a random node ID, arrival time, and
        size
    '''
    def __init__(self, packet_tuple):
        self.id = id
        self.packet_tuple = packet_tuple

    # Need a function that
